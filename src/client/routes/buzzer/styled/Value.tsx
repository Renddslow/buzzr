import styled from 'styled-components';

const Value = styled.div`
  position: absolute;
  top: calc(50% - 60px);
  width: 100%;
  display: block;
  text-align: center;
  left: 0;
  font-size: 120px;
  color: #363a4e;
  font-weight: 800;
`;

export default Value;
